'use strict';

import ReactNative, { AppRegistry } from 'react-native';
const ReactNativeIotLamp = require('./ReactNativeIotLamp');

AppRegistry.registerComponent('ReactNativeIotLamp', () => ReactNativeIotLamp);
